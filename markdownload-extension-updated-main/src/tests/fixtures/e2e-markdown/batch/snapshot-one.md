## Snapshot One Heading

Snapshot one paragraph for deterministic conversion checks.

This additional fixture text keeps the page above short-content retry thresholds so batch processing completes without false partial-capture retries during CI runs.
